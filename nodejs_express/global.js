// console.log(global);

// global.setTimeout(() => {
//     console.log('in the timeout');
// }, 3000);

// setTimeout(() => {
//     console.log('in the timeout');
// }, 3000);

// const int = setInterval(() =>{
//     console.log('Every Second');
// },1000);

// console.log(__dirname);//Gets current directory of our file or gets the absolute path of the current folder that global.js is in
// console.log(__filename);//Gets the absolute path with the filename as well
